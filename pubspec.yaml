name: virtual_catalog_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  firebase_core: ^4.4.0
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.12.0
  google_fonts: ^8.0.1
  smooth_page_indicator: ^2.0.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/ShurumbaImagenes/
